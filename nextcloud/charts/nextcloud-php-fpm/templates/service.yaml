apiVersion: v1
kind: Service
metadata:
  name: {{ include "nextcloud-php-fpm.fullname" . }}
  labels:
{{ include "nextcloud-php-fpm.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "nextcloud-php-fpm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
