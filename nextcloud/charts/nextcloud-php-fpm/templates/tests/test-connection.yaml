apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nextcloud-php-fpm.fullname" . }}-test-connection"
  labels:
{{ include "nextcloud-php-fpm.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "nextcloud-php-fpm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
